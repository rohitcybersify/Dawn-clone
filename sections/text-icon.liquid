<div class="textWithIcon">
   {% if section.settings.heading != blank %}
   <div class="Heading {{ section.settings.headingSize }}">{{ section.settings.heading }}</div>
     {% endif %}
  {% if section.settings.subHeading != blank %}
   <div class="SubHeading">{{ section.settings.subHeading }}</div>
     {% endif %}
   <div class="cardDiv {{ section.settings.textPosition }}">
   {% for block in section.blocks  %}
     <a href="" 
     <div class="iconImage">{{ block.settings.iconImage | img_url : '' }}</div>
     <div class="textWithIconTitle">{{ block.settings.title }}</div>
    <div class="textWithIconSubTitle">{{ block.settings.subTitle }}</div>
     <div class="textWithIconDescription">{{ block.settings.description }}</div>
     </a>
      {% endfor %}
     </div>
</div>
{% schema %}
  {
    "name": "Text icon",
    "settings": [
      {
        "type": "text",
         "id" : "heading",
          "label": "Heading"
      },
      {
        "type": "inline_richtext",
         "id" : "subHeading",
         "label": "Sub Heading"
      },
      {
        "type" : "select",
         "id" : "headingSize",
          "options": [
        {
          "value": "fs-3",
          "label": "Small"
        },
        {
          "value": "fs-2",
          "label": "Medium"
        },
        {
          "value": "fs-1",
          "label": "Large"
        }
      ],
      "default": "fs-1",
      "label": "Heading Size"
      },
     {
        "type" : "select",
         "id" : "textPosition",
          "options": [
        {
          "value": "d-flex",
          "label": "Right"
        },
        {
          "value": "bottom",
          "label": "Bottom"
        }
      ],
      "default": "bottom",
      "label": "Text position"
      }
    ],
    "blocks": [
      {
        "type" : "add_block",
         "name": "Block",
         "settings": [
           {
             "type": "image_picker",
              "id" : "iconImage",
               "label": "Icon Image"
           },
           {
             "type": "text",
              "id": "title",
               "label": "Title"
           },
           {
             "type": "inline_richtext",
              "id": "subTitle",
              "label": "Sub Title"
           },
           {
             "type": "richtext",
              "id": "description",
               "label": "Descrition"
           },
           {
             "type": "url",
              "id": "link",
              "label": "Link"
           }
         ]
      }
    ],
    "presets": [
      {
        "name" : "Text icon"
      }
    ]
  }
{% endschema %}